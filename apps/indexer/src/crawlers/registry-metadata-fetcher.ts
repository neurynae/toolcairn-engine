/**
 * Thin re-export shim. The authoritative fetcher now lives in
 * `@toolcairn/registry`. Existing indexer imports keep working unchanged.
 */
export { fetchRegistryMetadata } from '@toolcairn/registry';
